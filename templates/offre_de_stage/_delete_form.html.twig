<form method="post" action="{{ path('app_offre_de_stage_delete', {'id': offre_de_stage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_de_stage.id) }}">
    <button class="btn">Delete</button>
</form>
